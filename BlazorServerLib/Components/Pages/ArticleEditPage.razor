@page "/articles/edit-card/{ArticleId:int}"
@page "/articles/edit-card/"

@using BlazorWebLib.Components.Articles

@rendermode @(new InteractiveServerRenderMode(prerender: false))
@attribute [Authorize]

<ArticleEditComponent ArticleId="ArticleId ?? 0" />

@code {
    [Parameter]
    public int? ArticleId { get; set; }
}